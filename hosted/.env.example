# Oracle Autonomous Database
# For production: Use OCI Vault secrets instead of plain text
# See: https://docs.oracle.com/en-us/iaas/Content/KeyManagement/Tasks/managingsecrets.htm
ORACLE_USER=ADMIN
ORACLE_PASSWORD=${oci://vault/running-days/db-password}
ORACLE_CONNECTION_STRING=runningdays_tp
ORACLE_WALLET_PATH=/app/.wallet

# Sign in with Apple
# Store sensitive keys in OCI Vault
APPLE_CLIENT_ID=com.runningdays.hosted
APPLE_TEAM_ID=${oci://vault/running-days/apple-team-id}
APPLE_KEY_ID=${oci://vault/running-days/apple-key-id}
APPLE_PRIVATE_KEY=${oci://vault/running-days/apple-private-key}

# App Configuration
PUBLIC_APP_URL=https://running-days.app
SESSION_SECRET=${oci://vault/running-days/session-secret}

# OCI Vault Configuration (for secret resolution)
# The application will resolve ${oci://...} references at startup
OCI_VAULT_OCID=ocid1.vault.oc1...
OCI_VAULT_COMPARTMENT=ocid1.compartment.oc1...
