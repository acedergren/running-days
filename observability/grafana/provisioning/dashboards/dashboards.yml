apiVersion: 1

providers:
  - name: 'Running Days Dashboards'
    orgId: 1
    folder: 'Running Days'
    folderUid: 'running-days'
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
